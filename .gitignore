# Built and downloaded data - regenerated by the pipeline
data/
outputs/

# Raw archives behind the earlier analyses; the R scripts beside them are code
wwa_data/*_all/
# The Shiny app this site replaced: its script holds a Mapbox access token and
# rsconnect/ its deployment account, neither of which belongs in a public repo
wwa_data/wwa_shiny_app/
tsmf_wwa_data/
watch_data/*/
watch_data/*.csv
outlooks_data/*/
outlooks_data/*.csv
extended_outlooks_data/*/

.Rhistory
.RData
.Rproj.user/
.DS_Store
